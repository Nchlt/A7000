Version Untyped

main_integer(1 paramètres) (
  var Formal(1) x;
  var Local j;
  var Local i;
  var Local continue;
  continue := true;
  i := 0;
  while continue (
    continue := false;
    j := 0;
    while ( j < ( x + 1 ) ) (
      if ( ( ( i * i ) + ( j * j ) ) < ( x * x ) ) then (
        print(46);
        continue := true;
      ) else (
        print(35);
      );
      print(32);
      j := ( j + 1 );
    );
    print(10);
    i := ( i + 1 );
  );
)


